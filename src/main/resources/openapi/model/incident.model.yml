components:
  schemas:
    IncidentDto:
      properties:
        id:
          type: integer
          format: int32
        incidentType:
          type: string
        date:
          type: string
          format: date
        player:
          type: object
          $ref: "player.model.yml#/components/schemas/PlayerDto"
